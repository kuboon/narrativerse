<article id="link-<%= link.id %>" class="panel scene-container" data-link-id="<%= link.id %>" data-scene-id="<%= link.scene.id %>">
  <% owner = link.plot.user_id == current_user&.id %>
  <div class="scene-display <%= 'cursor-pointer hover:bg-gray-50' if owner %> p-2 rounded">
    <% if link.scene.text.present? %>
      <%= sanitize(link.scene.text, tags: %w[p strong h1 h2 ruby rt]) %>
    <% else %>
      （テキストを入力してください）
    <% end %>
  </div>

  <% if owner %>
    <div class="scene-editor hidden">
      <%= form_with model: link.scene, url: plot_scene_path(link), method: :patch, class: "scene-form w-full", html: { data: { turbo: true } } do |f| %>
        <div class="prosemirror-editor-container" data-controller="prosemirror">
          <div class="prosemirror-toolbar mb-2 flex gap-2" data-prosemirror-target="toolbar">
            <button type="button" class="px-2 py-1 border rounded" data-action="click->prosemirror#toggleBold" data-mark="strong">太字</button>
            <button type="button" class="px-2 py-1 border rounded" data-action="click->prosemirror#setRuby" data-mark="ruby">ルビ</button>
          </div>
          <%= f.hidden_field :text, class: "prosemirror-hidden-input scene-textarea" %>
          <div class="prosemirror-editor w-full p-2 border rounded min-h-[100px]" data-prosemirror-target="editor"></div>
        </div>
      <% end %>
    </div>
  <% end %>
</article>
