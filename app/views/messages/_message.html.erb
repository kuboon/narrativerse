<% presenter = MessagePresenter.new(message) %>

<div id="message_<%= message.id %>" class="chat <%= presenter.placement %>" data-message-kind="<%= presenter.thinking? ? 'thinking' : 'default' %>">
  <div class="chat-header text-xs font-semibold text-base-content/70">
    <%= presenter.speaker_name %>
    <time class="ml-2 text-[11px] font-normal text-base-content/50"><%= message.created_at&.strftime("%I:%M %p") %></time>
  </div>

  <% if presenter.thinking? %>
    <div class="chat-bubble chat-bubble-neutral max-w-[85%] p-0 overflow-hidden text-[11px] text-base-content/70">
      <details class="group">
        <summary class="flex items-center gap-2 p-2 px-3 cursor-pointer list-none hover:bg-base-content/5 transition-colors">
          <% if message.tool_call? %>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5 animate-spin" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M11.49 3.17c-.38-1.56-2.6-1.56-2.98 0a1.532 1.532 0 01-2.286.948c-1.372-.836-2.942.734-2.106 2.106.54.886.061 2.042-.947 2.287-1.561.379-1.561 2.6 0 2.978a1.532 1.532 0 01.947 2.287c-.836 1.372.734 2.942 2.106 2.106a1.532 1.532 0 012.287.947c.379 1.561 2.6 1.561 2.978 0a1.533 1.533 0 012.287-.947c1.372.836 2.942-.734 2.106-2.106a1.533 1.533 0 01.947-2.287c1.561-.379 1.561-2.6 0-2.978a1.532 1.532 0 01-.947-2.287c.836-1.372-.734-2.942-2.106-2.106a1.532 1.532 0 01-2.287-.947zM10 13a3 3 0 100-6 3 3 0 000 6z" clip-rule="evenodd" />
            </svg>
          <% elsif message.tool_result? %>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5 text-success" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
          <% end %>

          <span class="font-bold"><%= presenter.status_label %></span>
          <% if presenter.tool_result_summary.present? %>
            <span class="opacity-60 ml-1"><%= presenter.tool_result_summary %></span>
          <% elsif presenter.tool_names_label.present? %>
            <span class="opacity-60 ml-1">: <%= presenter.tool_names_label %></span>
          <% end %>

          <span class="loading loading-dots loading-xs group-open:hidden ml-auto" aria-hidden="true"></span>
          <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 ml-auto transition-transform group-open:rotate-180 opacity-50" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
          </svg>
        </summary>
        <div class="px-3 pb-2 pt-1 border-t border-base-content/5 bg-base-100/20 text-[10px] font-mono opacity-60">
          <% if message.tool_call? %>
            <%= message.tool_calls.map { |tc| tc.name }.join(", ") %>
          <% elsif message.tool_result? %>
            <%= message.parent_tool_call&.name %>
          <% elsif message.thinking_text.present? %>
            <span class="font-serif not-italic"><%= message.thinking_text.truncate(120) %></span>
          <% end %>
        </div>
      </details>
    </div>
    <% if message.role == "assistant" && message.content.present? && !message.tool_call? %>
      <div id="message_<%= message.id %>_content" class="chat-bubble <%= presenter.bubble_color %> max-w-[85%] whitespace-pre-wrap text-sm leading-relaxed mt-1"><%= message.content %></div>
    <% end %>
  <% elsif presenter.choice? %>
    <div class="chat-bubble chat-bubble-neutral max-w-[85%]">
      <%= render "messages/choice_buttons", payload: message.choice_payload %>
    </div>
  <% else %>
    <div id="message_<%= message.id %>_content" class="chat-bubble <%= presenter.bubble_color %> max-w-[85%] whitespace-pre-wrap text-sm leading-relaxed"><%= message.content %></div>
  <% end %>
</div>
