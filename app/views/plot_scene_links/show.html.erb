<main class="page">
  <section class="detail">
    <div class="detail-header">
      <div>
        <p class="eyebrow">Plot reader</p>
        <h1>Story</h1>
      </div>
    </div>
    <div class="empty-state">
      <p>The reader has moved. Use the new reader URLs instead.</p>
      <%= link_to "Go to reader", reader_path(params[:plot_id]), class: "text-link" %>
    </div>
  </section>
</main>
